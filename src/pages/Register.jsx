import {useState} from "react";import {Link,useNavigate} from "react-router-dom";
export default function Register({onLogin}){const[f,setF]=useState({name:"",email:"",password:""});const[error,setError]=useState("");const nav=useNavigate();const submit=async e=>{e.preventDefault();setError("");try{const r=await fetch("/api/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});const d=await r.json();if(!r.ok)throw Error(d.message);onLogin(d.user, d.token);nav("/");}catch(x){setError(x.message)}};return <main className="auth"><form className="auth-card" onSubmit={submit}><span className="eyebrow">JOIN THE KNOTS</span><h1>Create account</h1><p>Your account is required before buying.</p>{error&&<div className="error">{error}</div>}<label>Name<input value={f.name} onChange={e=>setF({...f,name:e.target.value})} required/></label><label>Email<input type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})} required/></label><label>Password<input type="password" minLength="6" value={f.password} onChange={e=>setF({...f,password:e.target.value})} required/></label><button className="btn primary wide">Create account</button><p className="switch">Already have an account? <Link to="/login">Login</Link></p></form></main>}
